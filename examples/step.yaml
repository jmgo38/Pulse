phases:
  - type: step
    duration: 60s
    from: 10
    to: 100
    steps: 5

target:
  method: GET
  url: http://localhost:8080

maxConcurrency: 150

thresholds:
  errorRate: 0.05
  maxMeanLatency: 500ms
